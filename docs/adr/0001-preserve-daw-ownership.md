# Preserve DAW ownership

HIT does not replace REAPER transport or ordinary editing. It wraps a native DAW action only when the action and HIT metadata must change atomically, as with HIT Split; otherwise the musician keeps using the DAW directly.
