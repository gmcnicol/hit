# Domain docs

This repository uses one domain context.

Before work, read `CONTEXT.md` and relevant ADRs under `docs/adr/` when present. Missing files require no setup: domain-modelling flows create them only when a term or durable decision needs recording.

Use glossary terms consistently. Surface conflicts with existing ADRs instead of silently overriding them.
